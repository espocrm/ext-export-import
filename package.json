{
  "name": "ext-export-import",
  "version": "1.0.2",
  "description": "Tool for export / import data of EspoCRM",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "EspoCRM",
  "dependencies": {
    "archiver": "^4.0.1",
    "fs-extra": "^9.0.0",
    "mv": "^2.1.1",
    "request": "^2.88.2",
    "unzipper": "^0.10.11"
  }
}
